## Лабораторная работа 1

1. **Шаг**

Пример вызова справочных команд для mkdir

![image](/uploads/c6718f657cc7480daec6f970e036aae8/image.png){width="752" height="298"}

Результат вызова man mkdir:

![image](/uploads/89d349e31dcc092b5a52fb839a40afe1/image.png){width="900" height="191"}

Результат вывода info mkdir:

![image](/uploads/532048a5a60c90fd625f91b1daabcfb9/image.png){width="900" height="345"}

2. **Шаг**

Вывод с экранированием. Для экранирования используется символ \\ перед специальными символами.

![image](/uploads/a2dd360e96bc3f602d227159cc05ae7d/image.png){width="880" height="198"}

Вывод в файл

![image](/uploads/944348048bef17b4eb572e63ad6c2107/image.png){width="900" height="122"}

Внутри одинарных кавычек ('') двойные кавычки не считаются спецсимволами экранирования, и внутренняя одинарная кавычка воспринимается как конец вводимой строки.

**Шаг 3. Результат выполнения команд:**

Cписок имен пользователей, работающих в системе, и их общее количество
`echo "who -q" > commands`

Текущая дата в формате: день месяц_полностью год
`echo 'date "+%d %B %Y"' >> commands`

Календарь за 1752 год в файл calendar
`echo 'cal 1752 > calendar' >> commands`

Вывести calendar с номерами непустых строк
`echo 'cat -b calendar' >> commands`

Подсчет слов в calendar
`echo 'wc -w calendar' >> commands`

Код возврата 0 указывает на корректное выполнение команды

**Шаг 4. Смена кодировки файла. Создание папки shell_lab1 и копирование файла.**

![image](/uploads/d967af925c418cfed7090bee4ea56928/image.png){width="900" height="129"}

В результате проверки командой `iconv -v <КОДИРОВКА>` содержимое файла выводится корректнo

Сравнение информации о файлах проводится по средствам команды `ls -l`